# ZealRAG 项目模拟面试（一）

- 面试日期：2026-07-30
- 面试主题：项目介绍、文档入库链路、RAG 检索链路、父子分块
- 结束原因：第 4 题暂时无法回答

## 总体评价

本轮回答已经覆盖了 RAG 的基本目标、文档分块、向量召回和 Rerank 等核心概念，说明对产品主流程有整体认识。但回答中仍存在以下问题：

1. 项目介绍偏抽象，没有突出 ZealRAG 相比普通 RAG Demo 的工程难点。
2. 文档入库链路中混淆了文档解析器、Embedding 模型和 LLM 的职责。
3. 没有掌握同步请求、异步任务、状态流转与失败恢复机制。
4. 对父子分块的基本目的理解正确，但没有讲清父块回溯、去重和 Token 预算控制。
5. “召回 50、Rerank 后保留 10”应说明为可配置参数，不能表达成固定规则。

---

## 第 1 题：项目整体介绍

### 问题

请你以面试者身份，在 3 分钟内介绍 ZealRAG，包括它解决的问题、核心功能、整体架构，以及你在项目中承担的主要工作。

### 候选人回答

ZealRAG 本质就是一个 RAG，它主要解决的是让 LLM 在回答问题时能够结合外部知识，而不是仅依赖模型参数中的知识。它主要解决了知识无法实时更新、专业领域知识不足、容易产生幻觉以及企业私有数据无法直接使用等问题。

核心功能是知识库以及重建时的结果复用。整体采用 Go 和 Vue。用户向知识库上传文件后，系统对文件进行分块并存入向量数据库。我们主要完成了从用户上传文档、数据入库，到用户提出问题并得到回答的完整流程。

### 面试官点评

优点：

- 能准确说明 RAG 解决的核心问题。
- 提到了知识实时更新、专业知识、幻觉和私有数据等典型应用价值。
- 提到了索引重建复用，这是 ZealRAG 比普通 RAG Demo 更有区分度的能力。

不足：

- “本质就是一个 RAG”过于简单，容易让面试官认为项目只是模型接口和向量数据库的拼接。
- 没有说明普通文档知识库、FAQ 知识库、混合检索、引用、联网搜索、异步处理等具体能力。
- 没有讲清 Go 后端、Vue 前端、PostgreSQL/pgvector、Redis 和文档解析服务之间的职责。
- 没有明确个人负责的模块、解决的难点和最终成果。

### 优化后的参考回答

ZealRAG 是一个面向企业内部资料和个人知识沉淀场景的本地化 RAG 知识库系统，主要解决大模型知识更新不及时、专业领域知识不足、回答容易产生幻觉，以及企业私有数据无法直接参与推理的问题。

系统支持普通文档和 FAQ 两类知识库，覆盖文件上传、文档解析、智能分块、摘要生成、向量化、关键词索引、混合召回、Rerank 重排、答案生成和原文引用的完整链路。用户提问时还可以选择是否联网搜索，用公开网页信息补充内部知识库内容。

项目后端采用 Go，前端采用 Vue 3 和 TypeScript；业务数据、文档元数据和分块存储在 PostgreSQL 中，向量通过 pgvector 管理，Redis 用于异步任务调度和状态处理，原始文件保存在本地文件系统中。耗时的解析、分块、摘要和向量构建通过异步任务执行，回答通过 SSE 流式返回。

我主要负责从文档入库到问答生成的完整业务链路，以及知识库完整重建能力。重建过程中设计了向量缓存复用、索引代际、完整性校验和原子切换机制，确保新索引构建失败时不会影响旧索引继续提供查询服务。

---

## 第 2 题：文档入库与检索链路

### 问题

请详细说明一个文档从用户上传到“可以被检索”的完整处理链路。请讲清楚原始文件、文档记录、分块、向量分别存在哪里，以及哪些步骤是同步执行、哪些步骤通过异步任务完成的。

### 候选人回答

文档上传时通过模型解析文档，然后采用父子块方式进行分块。分块后对内容进行向量化，并给每个分块向 LLM 提出三个问题，一起存入向量数据库。

用户输入问题后，先对问题进行向量化，再到向量数据库中检索并召回前 50 个向量。随后通过 Rerank 重排和进一步识别，最后保留 10 个左右的结果，排序后交给 LLM 生成回答。

向量存储在向量数据库中，其他数据存储在 SQL 数据库中。同步和异步执行过程暂时不清楚。

### 面试官点评

优点：

- 基本掌握了“解析 → 分块 → 向量化 → 召回 → 重排 → 生成”的主链路。
- 知道 Query 和文档需要进入同一 Embedding 空间。
- 知道初次召回数量通常大于最终交给 LLM 的上下文数量。

需要纠正：

1. 文档通常不是直接“通过 LLM 解析”。PDF、Word、Markdown 等文件首先由文档解析器抽取文本和结构；LLM 主要用于摘要、问题生成或答案生成等任务。
2. ZealRAG 的向量并不是存放在一个完全独立的数据库中，而是通过 PostgreSQL 的 pgvector 扩展管理。
3. 原始文件保存在本地文件系统；知识库、文档元数据、分块、任务状态等结构化数据存储在 PostgreSQL。
4. 并非所有普通文档分块都固定生成三个问题。问题生成是否执行、生成数量以及作用范围取决于知识库类型和处理配置，FAQ 问题生成是当前明确保留的能力。
5. 召回 50 条、Rerank 后保留 10 条是检索配置，不应表述为不可改变的固定值。

### 完整链路参考答案

文档上传请求首先执行同步步骤：校验知识库归属、文件类型和大小，检查重复文件，保存原始文件，并在 PostgreSQL 中创建文档记录。随后系统把文档处理任务写入 Redis 异步队列，接口可以在不等待整个解析过程完成的情况下返回。

异步 Worker 获取任务后，通过对应的文档解析器抽取正文、标题和表格等文本内容，再根据知识库配置执行普通分块或父子分块。分块及其页码、来源位置、父子关系等元数据写入 PostgreSQL。

之后系统调用 Embedding 模型把需要检索的文本转换为向量，并通过 pgvector 写入 PostgreSQL。处理过程中会更新文档的 `pending`、`processing`、`finalizing`、`completed` 或 `failed` 等状态，前端可以展示处理进度和失败原因。只有索引所需的数据完整写入后，文档才进入可检索状态。

用户提问时，系统先进行查询改写，再生成 Query Embedding，同时执行向量召回与关键词召回。两路结果融合后，根据配置执行 Rerank，筛选最相关的上下文，并与系统提示词、会话上下文一起交给 LLM。最终答案、引用、错误和完成事件通过 SSE 流式返回前端。

---

## 第 3 题：父子分块的作用

### 问题

你提到使用父子分块。为什么不直接使用固定大小的普通分块？父块和子块分别承担什么作用，哪一种用于向量检索，命中后最终应该把哪一种内容交给 LLM？

### 候选人回答

向量检索具有概率性。如果直接使用固定大小的块，很可能一句话还没有说完就被截断，导致语义与之前大不相同，最终召回错误内容。

子块是正常切割出的较小文本块，父块用于保证语义完整。子块用于向量检索，命中后把 Rerank 后的知识和系统提示词交给 LLM。

### 面试官点评

优点：

- 理解小块更适合精确匹配，大块更适合保留完整上下文。
- 知道父子分块的目标是在检索精度和上下文完整性之间取得平衡。

需要改进：

- 向量相似度检索不是严格意义上的“概率检索”，更准确的说法是根据向量空间中的相似度进行近似匹配。
- 固定大小分块的问题不仅是截断句子，还包括章节结构丢失、指代关系断裂、表格与标题分离等。
- 回答没有明确说明子块命中后是否回溯父块，以及 Rerank 的对象究竟是子块、父块还是组装后的上下文。

### 优化后的参考回答

普通固定大小分块实现简单，但容易在不合适的位置截断文本，造成标题与正文分离、指代关系缺失或一个完整论点被拆开。块太大时检索粒度不足，块太小时又缺乏回答问题所需的完整上下文。

父子分块通过两种粒度解决这个矛盾：子块较短，语义更集中，适合生成向量并参与精确召回；父块包含更完整的段落或章节上下文，用于在子块命中后补充信息。

检索时先使用子块获得高精度候选，再根据 `parent_id` 回溯对应父块或命中位置附近的上下文窗口。之后进行去重、重排和 Token 预算控制，最终把最相关且上下文完整的内容交给 LLM，而不是机械地把所有父块全部塞入提示词。

---

## 第 4 题：父块回溯、去重与 Token 预算

### 问题

如果多个被召回的子块属于同一个父块，你会如何进行父块回溯、去重和排序，同时避免重复上下文占满 LLM 的 Token 窗口？

### 候选人回答

暂时不知道。

### 参考答案

可以按照以下流程处理：

1. **按父块分组**：根据每个命中子块的 `parent_id` 分组，多个子块指向同一个父块时只加载一次父块内容。
2. **聚合相关度**：父块的分数可以取组内最高子块分数，也可以综合最高分、命中数量和 Rerank 分数，避免简单累加导致长文档天然占优。
3. **合并相邻命中**：如果命中的多个子块位置相邻，可以合并为一个连续上下文窗口；如果父块过大，则只截取命中位置前后的内容，而不是返回完整父块。
4. **内容去重**：使用分块 ID、文本哈希或文本区间判断重复，删除完全重复或高度重叠的内容。
5. **控制来源多样性**：可以限制每篇文档或每个父块最多贡献多少段内容，也可以使用 MMR 等方法兼顾相关性和结果多样性。
6. **执行 Token 预算**：按最终相关度排序，在系统提示词、历史消息和回答预留 Token 之外，为检索上下文设置明确预算；从高到低加入上下文，达到预算后停止。
7. **保留引用映射**：上下文合并或截断后仍需保留原始文档、页码和子块 ID，确保最终答案能够正确展示引用。

一个简化实现可以表示为：

```text
子块召回
  → 按 parent_id 分组
  → 聚合组内分数
  → 回溯父块或构造局部窗口
  → 去重与相邻区间合并
  → Rerank
  → 按 Token 预算截断
  → 组装提示词并保留引用
```

---

## 本轮需要重点复习的知识

1. 文档解析器、Embedding 模型、Rerank 模型和 LLM 各自承担什么职责。
2. 文档上传接口中的同步步骤与异步任务边界。
3. Redis 异步队列、任务重试、文档状态流转和失败恢复。
4. PostgreSQL、pgvector、本地文件系统分别保存哪些数据。
5. 向量召回、关键词召回、结果融合和 Rerank 的执行顺序。
6. 父子分块、父块回溯、相邻窗口合并、内容去重和 Token 预算控制。
7. ZealRAG 的索引代际、向量缓存复用、完整性校验和原子切换机制。

